<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Image Processing + VGG16 Classification</title>

  <!-- Load TensorFlow.js -->
  <script src="https://cdn.jsdelivr.net/npm/@tensorflow/tfjs@4.16.0"></script>

  <!-- Load VGG16 pre-trained model -->
  <script src="https://cdn.jsdelivr.net/npm/@tensorflow-models/vgg16"></script>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f3f3f3;
      padding: 20px;
    }
    h1 { text-align: center; }

    #container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
      margin-top: 20px;
    }

    .card {
      background: white;
      padding: 15px;
      border-radius: 10px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.14);
      text-align: center;
    }

    img {
      width: 100%;
      border-radius: 10px;
    }

    canvas {
      width: 100%;
      border-radius: 10px;
    }

    .controls {
      background: white;
      padding: 15px;
      border-radius: 10px;
      max-width: 480px;
      margin: 20px auto;
      box-shadow: 0 2px 6px rgba(0,0,0,0.14);
    }
  </style>
</head>
<body>

  <h1>Image Processing & VGG16 Classification</h1>

  <input type="file" id="imageInput" accept="image/*" multiple />

  <div class="controls">
    <h3>Bilateral Filter Parameters</h3>

    <label>Sigma Color: <span id="sigmaColorValue">80</span></label>
    <input type="range" id="sigmaColor" min="10" max="200" value="80">

    <br><br>

    <label>Sigma Space: <span id="sigmaSpaceValue">80</span></label>
    <input type="range" id="sigmaSpace" min="10" max="200" value="80">
  </div>

  <div id="container"></div>

  <!-- LINK SCRIPT.JS -->
  <script src="script.js"></script>

</body>
</html>
